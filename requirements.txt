# ============================================================
# RAICOM 2026 — 智海算法调优赛 依赖清单
# 安装: pip install -r requirements.txt
# ============================================================

# ---------- 深度学习框架 ----------
torch>=2.0.0
torchvision>=0.15.0

# ---------- 模型库 (EfficientNet) ----------
timm>=0.9.0

# ---------- 数据增强 ----------
albumentations>=1.3.0
opencv-python>=4.8.0

# ---------- 推理优化 ----------
onnx>=1.14.0
onnxruntime>=1.16.0           # CPU 推理核心

# ---------- 数据处理 ----------
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
pillow>=10.0.0
imageio>=2.31.0

# ---------- 进度与日志 ----------
tqdm>=4.66.0
tensorboard>=2.14.0           # 训练可视化（可选）

psutil>=5.9.0
pynvml>=11.5.0                # GPU/NVML 监控（可选，未安装则自动降级）
pyyaml>=6.0
